{
  "name": "evaluation-mock-api",
  "version": "1.0.0",
  "description": "Mock Experience API for technical note evaluation",
  "private": true,
  "main": "src/server.js",
  "scripts": {
    "sync-oas": "cp openapi.yaml docs/openapi.yaml",
    "prestart": "npm run sync-oas",
    "pretest": "npm run sync-oas",
    "start": "node src/server.js",
    "test": "node --test test/**/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
